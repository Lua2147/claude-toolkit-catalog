Stage all changes and commit with a descriptive message. Do NOT push.

1. `git status` to see changes
2. `git add` the relevant files (not -A, be specific)
3. Write a short descriptive commit message
4. Commit with Co-Authored-By trailer
5. Confirm success but do NOT push
