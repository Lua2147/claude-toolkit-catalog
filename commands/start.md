Load full monorepo context for a new session.

## Step 1: Read core context files (do all 3 in parallel)

Read these files from ~/Mundi Princeps/:
1. `AGENTS.md` — Monorepo reference, efficiency SOP, skills catalog, pre-build workflow, learnings log
2. `CLAUDE.md` — KOS sprint context, ClickUp IDs, Google IDs, current state, open items
3. `SESSION_PROTOCOL.md` — Mandatory end-of-session git commit workflow

## Step 2: Find and read the latest session log

Look in `~/Mundi Princeps/sessions/` for the most recent date folder. Read the latest .md file(s) inside it to understand what was done last session and what carries over.

## Step 3: Ask what we're working on

After loading context, ask the user: "What are we working on today?"

Do NOT summarize or recite the files back. Just load them silently and ask the question.
